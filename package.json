{
  "name": "kf-notes-be",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch",
    "cdb": "psql -f ./db/createDB.sql",
    "start": "node ./listen.js",
    "seedOld": "node db/data/sanitise-data/filter-old-data.js & node db/seeds/seed-db.js",
    "update": "node ./db/seeds/updateData.js",
    "seed:seedOld": "NODE_ENV=production DATABASE_URL=$(heroku config:get DATABASE_URL) npm run seedOld",
    "seed:update": "NODE_ENV=production DATABASE_URL=$(heroku config:get DATABASE_URL) npm run update"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^14.1.0",
    "express": "^4.17.2",
    "pg": "^8.7.1",
    "pg-format": "^1.0.4"
  },
  "devDependencies": {
    "jest": "^27.4.7",
    "supertest": "^6.2.2"
  }
}
